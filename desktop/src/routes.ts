/** Относительные пути от HTTP origin API (соответствуют роутерам сервера). */
export const ROUTES = {
  virtualMachinesClaim: "/virtual-machines/claim",
  wsConnectionStatus: "/ws/status",
} as const;
